# Web UI entry point — full implementation in Chunk 8.
# Stub keeps `plm-web` importable after `pip install -e .`


def main() -> None:
    raise NotImplementedError("Web UI not yet implemented (Chunk 8)")
